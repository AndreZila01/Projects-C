#include <stdio.h>

void main()
{
    int age;
    printf("Write your age: ");
    scanf("%d", &age);//callet pointer
    //fgest grab all line
    //fgests(age, {numero de quantas variaveis ele aceita}, stdin {?})
    printf("You have %d years old\n", age);
}