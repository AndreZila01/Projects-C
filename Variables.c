#include <stdio.h>

void main(){
    char name [] = "Adam";
    int age = 58;
    float moneyaccount = 01.5648786453485;

    printf("The client name is %s and he have %d years old\n", name, age);
    printf("%d\t%f\t%s\n", 500, moneyaccount, "moneyaccount"); 
    //%d print numbers round to 1 digit
    //%s print string
    //%f print double with zeros

    //pow(2,3) 2^3 not need a Math.Pow
    //sqrt(36) raiz de 36
    //ceil give a round hight number
    //floor give a round bottom number

    /*
    b=3
    a=++b; // a==4 and b ==4
    */
   /*
   b=3;
   a=b++; // a==3 and b==4
   */
}