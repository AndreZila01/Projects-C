#include <stdio.h>
#include <stdbool.h>
#include <string.h>
#include <stdlib.h>

void main()
{

    char *line = NULL;
    size_t len = 0;
    getline(&line, &len, stdin);
    if(line!=NULL){

    }
}